{{- $service := .Values.simpleRest }}
{{- $configServer := .Values.configServer }}
{{- $discoveryServer := .Values.serviceDiscovery }}
{{- $image := printf "%s:%s" $service.image .Values.general.imageTag }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Release.Name }}-{{ $service.name }}
  namespace: {{ .Release.Namespace }}
  labels:
    app: {{ .Release.Name }}-{{ $service.name }}
spec:
  replicas: {{ $service.replicas }}
  selector:
    matchLabels:
      app: {{ .Release.Name }}-{{ $service.name }}
  template:
    metadata:
      labels:
        app: {{ .Release.Name }}-{{ $service.name }}
    spec:
      {{- with $service.nodeSel }}
      nodeSelector:
{{ toYaml . | indent 8 }}
      {{- end }}
      containers:
        - startupProbe:
            httpGet:
              path: /actuator/health/liveness
              port: {{ $service.containerPort }}
            initialDelaySeconds: {{ .Values.general.minimumStartupDelayInSecs }}
            periodSeconds: {{ .Values.general.startupProbePeriod }}
            failureThreshold: {{ .Values.general.startupFailureThreshold }}
          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: {{ $service.containerPort }}
            periodSeconds: {{ .Values.general.livenessProbePeriod }}
            failureThreshold: {{ .Values.general.livenessFailureThreshold }}
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: {{ $service.containerPort }}
            periodSeconds: {{ .Values.general.readinessProbePeriod }}
            failureThreshold: {{ .Values.general.readinessFailureThreshold }}
          resources:
            requests:
              memory: {{ $service.requests.mem }}
              cpu: {{ $service.requests.cpu }}
          name: {{ $service.name }}
          image: {{ $image }}
          imagePullPolicy: {{ .Values.general.imagePullPolicy }}
          ports:
            - containerPort: {{ $service.containerPort }}
          env:
            - name: CONFIG_SERVER_URL
              value: "http://{{ .Release.Name }}-{{ $configServer.name }}.{{ .Release.Namespace }}:{{ $configServer.clusterPort }}"
            - name: DISCOVERY_SERVICES
              value: "http://{{ .Release.Name }}-{{ $discoveryServer.name }}.{{ .Release.Namespace }}:{{ $discoveryServer.clusterPort }}/eureka/"
            - name: SPRING_PROFILES_ACTIVE
              value: "dev"
            - name: EXCHANGE_RATE_SERVICE_NAME
              value: "exchange-rate"
            - name: CURRENCY_SERVICE_NAME
              value: "currency"
---
apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-{{ $service.name }}
  namespace: {{ .Release.Namespace }}
spec:
  type: ClusterIP
  selector:
    app: {{ .Release.Name }}-{{ $service.name }}
  ports:
    - protocol: TCP
      port: {{ $service.clusterPort }}
      targetPort: {{ $service.containerPort }}
---
{{- if $service.externalAccess.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-{{ $service.name }}-ext-access
  namespace: {{ .Release.Namespace }}
spec:
  type: NodePort
  selector:
    app: {{ .Release.Name }}-{{ $service.name }}
  ports:
      - protocol: TCP
        nodePort: {{ $service.externalAccess.port }}
        port: {{ $service.clusterPort }}
        targetPort: {{ $service.containerPort }}
---
{{- end }}