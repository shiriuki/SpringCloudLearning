# Run the minikube command first. It sets these environment variables:
# DOCKER_HOST, DOCKER_CERT_PATH
# which are needed to connect to it as a replacement of Docker Desktop.
minikube docker-env
gradle buildImage